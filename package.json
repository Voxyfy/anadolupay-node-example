{
  "name": "anadolupay-node-example",
  "version": "0.1.0",
  "private": true,
  "description": "@voxyfy/anadolupay (Node.js) paketini gerçek banka/sağlayıcı test ortamlarına karşı denemek için Express + React örnek projesi",
  "engines": {
    "node": ">=20 <21"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
